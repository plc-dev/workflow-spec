# Minimal single-node kind cluster used ONLY as a local, ephemeral,
# real-Kubernetes target for task 6.13's injection-mechanism test (T14,
# docs/impl-plans/0010-exec-agent.md). Not a dev/staging/prod cluster
# shape - a single control-plane node is enough to exercise
# init-containers, emptyDir, and command overrides for real.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
name: exec-agent-e2e
nodes:
  - role: control-plane
