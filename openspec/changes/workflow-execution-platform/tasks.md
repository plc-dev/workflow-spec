> **Note on paths and the software-design ADR series.** The real, tested spike-era
> components this file references by path (`dsl/`, `registry/`, `placement-resolver/`,
> `spikes/*`, `scripts/`) have been moved to `archive/dsl/`, `archive/registry/`,
> `archive/placement-resolver/`, `archive/spikes/*`, `archive/scripts/` - preserved as
> reference/inspiration, not as a starting point to build on directly. `docs/adr/`
> (0001-0010) now records the software-design decisions - repository/module structure,
> language and tooling, the execution-plan contract, binding resolution, the CLI-dispatch mechanism,
> the in-pod exec-agent, and CI/dev-environment tooling - that govern how the tasks below
> are actually implemented. Several tasks below are corrected in place (not merely
> re-pointed) where an ADR decision changed a task's scope; see the inline notes.
>
> **`docs/adr/0005`/`0008` correction, applying to every CLI/heavy-data/warm-pooling task
> below**: CLI dispatch is now the engine's universal step-invocation transport (every
> registered function, not a "CLI-invoked" subset) - see design.md D17a. Tasks 2.12, 4.8,
> 6.3, 6.4, 7.1, 7.2, 9.3, and 9.4 are corrected accordingly. This surfaced a real
> compatibility gap - **RESOLVED**: every onboarded service must ship a native CLI, no
> platform-generated wrapper exception (design.md D17a) - with a concrete, actionable
> consequence recorded on task 1.1 below.

## 1. Foundations & spikes

- [x] 1.1 Confirm COW/incremental-snapshot capability for the SQL-execution service end to end (proof of concept); SQL-Service-image: ghcr.io/htw-aladin/sql-assessment-service:sha-23e9468 ; SQL-Service-Code: https://github.com/HTW-ALADIN/ALADIN-Services/tree/master/services/sql-assessment-service. Done: see `archive/spikes/1.1-cow-snapshot-poc/FINDINGS.md` - verdict: this specific service does NOT support COW/incremental snapshotting (confirmed by direct measurement against the real pinned image: no cross-instance caching for byte-identical seed content, same-key re-analyze fully replaces rather than incrementally updates, and no snapshot/dump/fork/clone endpoint exists anywhere in its actual API surface). Register this image with `cowSupport: false` (D1/D5) - it belongs to the full-copy fallback materialization class (3.5), not the COW-capable class (3.4). A legitimate negative result, not a failed spike. **NEW FINDING (this round, design.md D17a):** this spike's own probes were all HTTP (`curl ... -X POST` against the service's REST API) - no CLI invocation path for this service was found or tested. Under the now-universal CLI-dispatch mandate (`docs/adr/0005`, design.md D17a) and the decision that no platform-generated CLI wrapper substitutes for a native one, **this service cannot be onboarded to this platform in its current, REST-only shipped form.** A follow-up task (adding a native CLI entrypoint to the upstream service, satisfying the `--data-file`/`--state-id` contract) is required before this reference service can actually be registered/dispatched - track this alongside 2.3's backfill task, not as a blocker to the software-design work itself.
- [x] 1.2 PRIMARY, DEEPEST SPIKE: fork a documented Postgres-durable-execution pattern (e.g. resonate-pg) and run the SQL-session scenario against it, explicitly testing whether the placement-resolver (1.10) and session log (3.1) can genuinely share one Postgres instance/transaction with the durability layer - i.e. testing the 4-way infra-consolidation claim at its DEEP end (same-transaction atomicity across concerns, not just same-instance locality), plus D3's linear-per-session-mutation guarantee under concurrent/crash conditions via ordinary `SELECT...FOR UPDATE` discipline (path f, design.md D6) - per D6's narrowed recommended next step (spike this first). Done: see `spikes/1.2-resonate-pg-durable-exec/` (schema + worker + happy-path/contention/crash tests, all passing against a real Postgres instance) and its `FINDINGS.md` - verdict: same-transaction atomicity across all four concerns holds, demonstrated via a mid-transaction-crash test (not just the uncontended happy path, which alone can't distinguish DEEP from SHALLOW); D3's linear-per-session-mutation guarantee holds under same-session concurrency and under two distinct crash/dead-worker failure shapes; cross-session non-contention is checked for correctness (two interleaved sessions, one shared worker pool, no cross-contamination) but lock-wait timing itself wasn't instrumented - see FINDINGS.md caveats.
- [x] 1.2-hatchet SHALLOW, PRODUCT-FIT EVALUATION: Hatchet, scoped to one decisive question before any full build-out - does its worker/step-completion model expose a shared-transaction boundary (letting a step-completion and a placement/session-log write commit atomically), or is completion reported via an out-of-process API callback (two commits, not one)? The former would make Hatchet a genuine contender for DEEP consolidation with someone else owning the durability core; the latter caps it at SHALLOW consolidation (shared Postgres instance/ops only) and reframes the choice against 1.2 as an explicit build-(resonate-pg)-vs-buy-(Hatchet) trade rather than a technical unknown a spike resolves - mirrors the lightweight-evaluation treatment given to Conductor in 1.2d. Done: see `spikes/1.2-hatchet-product-fit/FINDINGS.md` - verdict: out-of-process gRPC callback into the Engine's own Postgres transaction (two commits, not one); Hatchet caps at SHALLOW consolidation.
- [x] 1.2a RE-SCOPED from NARROW, TARGETED SPIKE to a LIGHTWEIGHT, DESK-RESEARCH EVALUATION (same posture as 1.2-hatchet/1.2d): Restate. The one differentiated capability claim this was scoped to check - whether the Postgres-native path can match Restate's "per-key serialized access for free" via ordinary `SELECT...FOR UPDATE` discipline without surprises - has already been answered empirically by spike 1.2's own contention test (same-session serialization + cross-session non-contamination confirmed, no surprises found). No full parallel build-out against Restate is justified by an open technical question anymore; confirm via Restate's own docs/architecture that no other differentiator applies beyond what design.md D6 already found (Virtual Objects can't host the heavyweight SQL service itself, only bookkeeping), then close out without further spike effort. Per design.md D6's updated "Recommended next step." Done: see `spikes/1.2a-restate-lightweight-eval/FINDINGS.md` - verdict: no new differentiator found; Virtual Objects confirmed unable to host the heavyweight SQL service (matches existing D6 finding); the one correction is a downward revision of Restate's "Light" operational-weight rating for production topologies (distributed log + Raft/etcd/S3 metadata + RocksDB + object-store snapshots + a K8s operator) - which, if anything, widens the gap in favor of the Postgres-native path's operational-weight claim rather than narrowing it.
- [x] 1.2e NEW - LOAD/SCALE CHECK for the Postgres-native path (path f, design.md D6): the one claim spike 1.2 did not test. Narrowly scoped (like 1.2a was originally), not a production-readiness project - higher worker/execution-row counts than spike 1.2 used, watching for lock contention, connection-count ceilings, and vacuum/bloat under sustained UPDATE churn on `executions`. This is the last open item before treating the Postgres-native path's "lightest operational footprint" claim as more than a paper claim, and should close before 1.4 locks in a recommendation. Done: see `spikes/1.2-resonate-pg-durable-exec/FINDINGS-1.2e-load-scale.md` - verdict: no showstopper at ~75x spike 1.2's original scale (6,000 executions/60 sessions/32 workers): all executions processed exactly once, all session chains stayed contiguous, peak connections (33) stayed well under `max_connections` (100), and dead tuples generated by a dedicated UPDATE-churn phase were fully reclaimable via `VACUUM`. Scope caveat: single-instance/single-machine, seconds-scale test, not a production capacity-planning exercise.
- [x] 1.2b CLOSED, CONFIRMED NOT REQUIRED (was: spike a Temporal-shaped engine + a hand-built placement-resolver for R11 as the fallback/baseline comparison, path a, design.md D6). This existed only as a baseline/fallback in case the Postgres-native path didn't hold up, or as a hedge while D6 was still open. Neither applies anymore: 1.2/1.2e empirically confirmed the Postgres-native path's claims via real crash/contention/load testing, and D6/D6a is now a locked-in decision, not an open question needing a baseline comparison. Closed without spiking - see design.md D6's updated closure note for the full rationale.
- [x] 1.2c CLOSED, CONFIRMED NOT REQUIRED (was: deferred, spike Dapr (Workflows + Actors) in placement-bookkeeping-only mode if 1.2 and the Restate evaluation together surfaced a blocker neither resolves). That trigger never fired - 1.2 confirmed DEEP consolidation and 1.2a found no differentiator Dapr could plausibly offer instead (both Restate and Dapr are limited to placement-bookkeeping-only actors against the heavyweight SQL service, per D6's own finding). Closed as confirmed-not-required rather than left as a standing contingency with nothing left to be a contingency for. Its one standing differentiator (a native Secrets API) remains a nice-to-have against the already-decided, broker-agnostic D7 - not a reason to reopen this.
- [x] 1.2d Lightweight evaluation of Conductor (Orkes): how directly its native MCP gateway (Agentspan) and declarative workflow format could serve D9c and the execution-plan-to-engine compilation step (5.10), before committing to a full spike (path e, design.md D6). Done: see `spikes/1.2d-conductor-lightweight-eval/FINDINGS.md` - verdict: substantiates, not overturns, D6's existing "strongest R12 / lowest-effort compilation target" rating - `SWITCH`/`branch` and single-task-name `FORK_JOIN_DYNAMIC`/`map` are genuinely close structural matches. Surfaces three compilation-detail refinements for 5.9/5.10 (compile to the single-task-name fork form; inline/expand forked workflow-specs rather than emit `SUB_WORKFLOW` live references; generate two separate Conductor artifacts for the derived signature), and confirms no native durable-governor-counter primitive exists (same conclusion as every other candidate) - none of these are blockers or new open questions.
- [x] 1.4 Decide the execution engine (design.md D6, open question) - resolve alongside 1.6 (secrets-broker fit, e.g. Dapr's native Secrets API) and 1.9 (composability cost), since both depend on the engine chosen. **DECIDED: a Postgres-native path implementing THE PATTERN** - locked in via spikes 1.2/1.2-hatchet/1.2a/1.2d/1.2e; see design.md D6's new "DECIDED" status for the rationale. 1.6 and 1.9 remain open and can now proceed against a concrete engine choice instead of a hypothetical one
- [x] 1.4a Decide the concrete fork target (design.md D6a, follow-up to 1.4): resonate-pg, hatchet-dev's tutorial, or clean-room. **DECIDED: clean-room** - neither reference implementation ships our actual differentiating requirement (D3/D4/D6 sharing one transaction); resonate-pg is 3 weeks old and Supabase/pg_net-transport-coupled, hatchet-dev's tutorial is Go-stacked and explicitly missing durable sleep/LISTEN-NOTIFY/forking. Spike 1.2's existing schema.sql/worker.js (already crash/contention/load-tested) is the actual starting point for 6.1/6.2, not a throwaway to be replaced by a fork. Both remain valuable as design references going forward (e.g. for the eventual `waits`/durable-sleep table). See design.md D6a for the full comparison.
- [x] 1.5 Spike compiling a trivial hand-written workflow-spec into a generic, engine-agnostic execution-plan interpreter targeting whichever engine is selected in 1.4. Done: see `spikes/1.5-ir-interpreter/` and its `FINDINGS.md` (archived under its original name; see `docs/glossary.md` for the "IR" -> "execution plan" terminology rename) - a hand-written execution-plan document (two-case branch + map/forEach + a step depending on both) run against a generic interpreter built on the SAME `executions`/`checkpoints`/`claim_execution()` pattern as spikes 1.2/1.8. Verified (14/14 assertions passing, stable across repeated runs against a real Postgres instance): dependency ordering is enforced from DB state alone (a node with unmet `{from:"step"}` dependencies starts `blocked`, is promoted to `queued` only once satisfied - in the same transaction as the write that satisfies it); branch-case exclusivity holds in both directions (the unselected case's step is never even created, not just unexecuted); map join preserves original source order under real concurrency. A real bug (reading a branch's `yields` from the wrong place - it's declared per-case, not once on the branch, per D8c) was caught by actually running this, not by review. Explicitly out of scope (stated, not discovered as gaps later): multi-step branch cases/map bodies, `session`/`static` bindings, real service dispatch, and 5.11's JSON-Logic-vs-CEL choice - see FINDINGS.md for what each of these would require on top of this pattern.
- [x] 1.6 Select a secrets-broker product against the broker-agnostic model in design.md D7 (open question). Done: see `spikes/1.6-secrets-broker-eval/FINDINGS.md` - verdict: **OpenBao**, chosen over Infisical because D7 rule 5 (session-TTL-bound, broker-enforced secret revocation) is native and free in OpenBao's core (lease/TTL/expiration-manager mechanics) but Enterprise-gated in Infisical (Dynamic Secrets sit behind Infisical's paid `ee/` tier even self-hosted); OpenBao's path-based ACL policies and free Namespaces also give writer/session isolation a more structural home than Infisical's core tier. Accepted cost: OpenBao is the operationally heavier of the two to run (own storage backend, unseal/init lifecycle) - accepted as consistent with this platform's existing posture of self-operating its own Postgres-native durability layer (D6/1.2e).
- [x] 1.7 Produce the formal JSON Schema for the authoring-surface grammar decided in design.md D8a/D8c (step invocation, secrets, `literal` bindings, and `branch`/`map`'s `yields` mechanism) - no further open syntax decisions remain, this is transcription/tooling work. Done: see `dsl/schema/workflow-spec.schema.json` (JSON Schema draft 2020-12) + `dsl/schema/README.md` - implements the full `WorkflowSpec`/`Binding`/`Step`/`branch`/`map` grammar with digest-only service refs, flat request params, and secrets as a separate category from bindings. Verified: `npm test` (ajv) - 9/9 passed (5 valid examples pass, 4 invalid examples correctly rejected). Explicitly documents that the restricted-YAML profile (anchors/aliases/merge-keys) is NOT and cannot be enforced by this schema - that belongs to task 5.6a's parser.
- [x] 1.8 Spike a dynamic map/forEach construct against the engine selected in 1.4 (design.md D8/D9). Done: see `spikes/1.8-map-foreach/` and its `FINDINGS.md` - verdict: a map child is literally just another `executions` row (parent_execution_id + map_index columns), claimed/retried/checkpointed by the SAME `claim_execution()` machinery as any other execution - no bespoke fan-out engine needed. Verified (19/19 assertions passing against a real Postgres instance): fan-out creates N independently claimable children; one child's injected failure+retry doesn't affect any other child; a shared worker pool interleaves map children with unrelated standalone work (parent never blocks/babysits); the join collects results in original source order despite genuinely out-of-order concurrent completion. Caveats: injected failure only (not a true crash/lease-recovery test), single-field yields only, no stuck-parent reconciliation sweeper, no scale/throughput measurement.
- [x] 1.9 Decide the service-nesting policy (deferred, design.md D9): SDK-mediated only vs. permitting direct HTTP/CLI/MCP transports - resolve alongside 1.4. **DECIDED: no bespoke SDK required.** D9b's mechanism-cost question (Temporal's separate child-workflow SDK vs. Restate/Dapr's "same primitive as any call") is moot for a clean-room engine we built ourselves (D6a) - there's no third party's client protocol to adopt. HTTP/CLI/MCP are three thin projections of ONE platform-owned dispatch primitive (a POST/CLI-call/MCP-tool-call to our own dispatch endpoint, which inserts the tracked child execution - never a call to the target's raw endpoint), mirroring D9c's already-decided framing for the target side. `sdk` remains a valid, optional `via` value (already implemented in `registry/src/validate.js`) for services that want a thin generated convenience wrapper, never a compliance requirement. See design.md D9b's new resolution note.
- [x] 1.10 Design the placement-resolver/routing mechanism (design.md D4/D6 Open Questions): bespoke resolver, service-mesh consistent-hash policy, or a native engine primitive, depending on the outcome of 1.4. If 1.4 selects the Postgres-native path, spike 1.2's `placement` table (content_hash -> replica_id, upserted in the same transaction as the durability core and session log) is a working draft of the bespoke-resolver option, not a from-scratch design - formalizing it (capacity/promotion fields for 4.x) should be materially faster than the other two options. Done: see `placement-resolver/` and its `FINDINGS.md` - the bespoke resolver, formalized: a `placement` table plus a data-driven `placement_config` (D4a's thresholds as tunable data, never hardcoded) and a `placement_access` rolling-window event log. Implements `resolvePlacement`/`recordAccess`/`evaluatePromotion`/`evaluateDemotion`/`promote`/`demote`/`evictLRUIfOverCapacity`, with decision/action separated and a declared-cost-prior-until-enough-observed-samples rule (D5). Verified (30/30 assertions passing against a real Postgres instance): batch bindings never auto-promote regardless of frequency; promotion/demotion hysteresis gap holds; LRU eviction only touches the pinned set, never unpinned entries; `recordAccess` accepts a caller-owned transaction client so it can share a transaction with durability/session-log writes, matching spike 1.2's DEEP-consolidation pattern. Trust-tier gating is exposed (`isTrustEligibleForOptimization`) but deliberately not self-enforced - the caller (4.7) is responsible for checking it, sourced from the registry's `getPlacementFacts` (task 2.x). Makes tasks 4.4-4.7 materially faster, as this task predicted.

- [x] 1.11 NEW - Decide how writer/end-user identity is established and reaches the platform (surfaced by tracing a concrete secrets scenario end to end - D7/D13 both depend on an identity neither decision specified how to obtain). **DECIDED (design.md D14): the platform is a relying party, never an identity issuer.** Writer identity is authenticated entirely outside the platform (by whatever the external authoring tool already needs for its own login/tenancy UX, per D13's existing layering); the platform only verifies an incoming identity assertion and extracts an OPAQUE `writerId` string, used purely as a scoping key (no platform-owned `writers` table, no profile data). End-user identity is not a platform concept at all - only SESSION identity (D2/D3, already decided) is needed anywhere D7 requires "the end-user," since user-scoped secrets are scoped to the session, not to a durable cross-session account, per D7's own scope table. Left genuinely open (tracked in design.md's Open Questions, not silently assumed): the concrete verification mechanism (JWT+JWKS vs. API key vs. mTLS), and whether an identity provider already exists in the broader product context (the funded UI/authoring-tool project, or the ecosystem task 1.1's SQL-execution service came from) that this should integrate with rather than stand up fresh.

## 2. Service registry

- [x] 2.1 Design and build the service-registry metadata index (design.md D12): per-image-digest OpenAPI contract as the sole stored contract (CLI/MCP surfaces projected from it, never stored separately), `oci_ref` pointer into a standard OCI-compliant registry (byte storage itself deferred). Done: see `docs/impl-plans/0007-registry.md` - promoted from `archive/registry/` into `src/registry/` (ADR-0006/0007/0012, own database, entirely separate from `core/`) - `src/registry/database/schema.sql`'s `service_images` table keyed by digest, storing `openapi_spec` (JSONB, sole contract) and `oci_ref`, exposed via `src/registry/get-entry.ts`. Tests: `test/registry/database/schema.test.ts`, `test/registry/get-entry.test.ts`.
- [x] 2.1a Extend the entry schema to carry per-function capability metadata: mutates, materialization-cost-class, COW-support, change-detection-support (design.md D5). Done: see `docs/impl-plans/0007-registry.md` - `src/registry/database/schema.sql`'s `function_capabilities` table, composite key (digest, function_name); `materialization_cost_class` uses D1's exact `negligible|heavy` enum (`src/registry/constants.ts`). Tests: `test/registry/repositories/function-capabilities.repository.test.ts`.
- [x] 2.1b Extend the entry schema to carry per-image hardware requirements (cpu/mem/gpu/node-class), explicitly outside the capability trust-tier model (design.md D12). Done: see `docs/impl-plans/0007-registry.md` - `hardware_requirements` JSONB column on `service_images`, a genuinely separate column-set from `trust_tier`. Tests: `test/registry/get-placement-facts.test.ts`.
- [x] 2.1c Extend the entry schema to carry a per-function `nesting_declaration` (transport + enumerable/open targets) recording only the possibility of nesting, not the concrete bound target (design.md D9b/D12). Done: see `docs/impl-plans/0007-registry.md` - `nesting_declaration` JSONB on `function_capabilities` (`src/registry/domain/nesting-declaration.ts`), validated by 2.2. Tests: `test/registry/repositories/function-capabilities.repository.test.ts`.
- [x] 2.2 Add registry validation for capability/hardware/nesting metadata (schema-level checks). Done: see `docs/impl-plans/0007-registry.md` - `src/registry/validate.ts` - enum/required-field checks plus a referential check that capability metadata's function keys actually exist in the entry's own `openapi_spec` operations. Tests: `test/registry/validate.test.ts`.
- [ ] 2.3 Backfill capability, hardware, and nesting metadata for existing service images. Deferred (see `docs/impl-plans/0007-registry.md`'s Scope): no service images are registered yet in this repo to backfill against - `registerImage` (`src/registry/admin.ts`, 2.1/2.10) is exactly what a future backfill would drive, once real images exist.
- [ ] 2.4 Define conformance checks/tests to validate declared capabilities against actual service behavior (trust boundary from design.md). Deferred: needs real running service images to probe declared behavior against - a substantially larger effort than this pass's scope; 2.5's tier storage (`src/registry/conformance.ts`) is what this task would write into once built.
- [x] 2.5 Implement capability declaration trust tiers keyed to image digest (unverified / conformance-passed / production-proven, design.md D5a). Done: see `docs/impl-plans/0007-registry.md` - `trust_tier` column with a CHECK-constrained enum, defaulting every new registration to `unverified`; `src/registry/conformance.ts`'s `recordTrustTier(pool, digest, tier)` transitions it (see 2.10 for its privilege split). Note: this is the tier *storage/transition* only - the actual conformance-probing logic that would call it (2.4) remains deferred, as noted there. Tests: `test/registry/conformance.test.ts`.
- [ ] 2.6 Gate conformance re-checks into CI/CD on every service redeploy, not just first registration (design.md D5a). Deferred: depends on 2.4 (a real conformance pipeline) plus actual CI wiring - pipeline integration, not registry logic. A redeploy already produces a new digest defaulting to `unverified` per 2.5's schema, satisfying D5a's "no inherited trust" rule structurally; the re-check *trigger* itself is what remains to be built.
- [ ] 2.7 Implement a continuous runtime invariant checker sampling shared/immutable bindings for cross-caller divergence, with auto-demotion + alerting on violation (design.md D5a). Deferred: needs real traffic to sample for cross-caller divergence; once built, it is simply another caller of the already-implemented `recordTrustTier` (2.5, `src/registry/conformance.ts`).
- [x] 2.8 Implement an atomic `getPlacementFacts(digest, function)` read returning capability metadata, trust tier, and hardware requirements together, so callers never observe them skewed relative to one another (design.md D12). Done: see `docs/impl-plans/0007-registry.md` - `src/registry/get-placement-facts.ts` + `src/registry/repositories/queries/get-placement-facts.queries.ts`'s `SQL_GET_PLACEMENT_FACTS` - a single SQL query (one JOIN, one MVCC snapshot), not multiple round-trips composed in application code. Tests: `test/registry/get-placement-facts.test.ts`.
- [ ] 2.9 Implement digest-pinned resolution for workflow-spec step bindings at authoring time (design.md D12). Deferred: this is really a DSL-compiler concern (task 5.3) - the registry already *exposes* digest-pinned entries by being keyed on digest (`getEntry`/`getPlacementFacts`, 2.1/2.8), so no DSL-authoring-tool logic was built here.
- [x] 2.10 Implement privilege-split write paths: `registerImage` reachable only by platform developers; `recordTrustTier` reachable by the workflow platform's own conformance pipeline, not requiring developer involvement per update (design.md D12). Done: see `docs/impl-plans/0007-registry.md` - the split is structural, not a runtime permission check - `src/registry/admin.ts` exports ONLY `registerImage`, `src/registry/conformance.ts` exports ONLY `recordTrustTier`, and `src/registry/index.ts`'s barrel re-exports neither; nothing in a hypothetical runtime-facing module could import/call `registerImage` via the barrel. No real auth/RBAC system exists yet in this repo (out of scope here) - this establishes the structural boundary a future auth layer would enforce. Tests: `test/registry/index.test.ts`, `test/registry/admin.test.ts`.
- [ ] 2.11 Design the deferred re-pin/upgrade flow for moving an already-authored binding to a newer image digest (design.md D12, noted as a real but not-yet-designed affordance). Deferred: design.md D12 itself flags this as "not yet designed" - inventing it now would pre-empt an unmade decision rather than implement an already-made one.
- [ ] 2.12 **CORRECTED SCOPE (design.md D17a, `docs/adr/0005`):** extend the registry's onboarding requirements with the mandated CLI heavy-data transport contract (`--data-file <path> --state-id <key>`, universal, not per-function-discovered) for **every registered function** (CLI is now the engine's universal step-dispatch transport - not a subset of "CLI-invoked" functions), applying `--data-file`/`--state-id` whenever a given call's binding happens to be heavy/dataset-scoped and ordinary CLI flags otherwise (design.md D17/D17a)

## 3. Session & state layer

- [x] 3.1 Implement durable session input-history log (append-only, per session) - evaluate whether this can be built directly on the selected engine's own durable execution history (design.md D3 note) rather than as separate infrastructure. Done: see `docs/impl-plans/0003-session-log.md` - resolved the "evaluate" clause: NO, a dedicated `session_log` table is needed (a session's arbitrary-cardinality action history outlives and doesn't map 1:1 onto any single `executions` row's lease/retry lifecycle), consistent with ADR-0002's own schema diagram. `src/core/database/schema.sql` (`session_log`/`session_pointer` tables), `src/core/domain/{session-log-entry,session-pointer}.ts`, `src/core/repositories/{session-log,session-pointer}.repository.ts` + `repositories/queries/{session-log,session-pointer}.queries.ts`, `CoreRepos.{sessionLog,sessionPointer}`, and the new `src/session/` module's `session-log.ts` (`appendEntry`/`replaySession`). Tests: `test/core/database/schema.test.ts` (extended), `test/core/repositories/{session-log,session-pointer}.repository.test.ts`, `test/session/session-log.test.ts` (60/60 tests passing across 14 files, real testcontainers-managed Postgres) - covering ordered append, lazy pointer creation, same-session-serializes/cross-session-doesn't-contend concurrency (with an actual blocking-proof test for the latter), and `replaySession` correctly excluding a rewind's not-yet-truncated abandoned tail.
- [ ] 3.2 Implement content-addressed snapshot store (hash of base + operations -> snapshot)
- [ ] 3.3 Implement linear snapshot chain construction per session
- [ ] 3.4 Implement copy-on-write materialization path for COW-capable services
- [ ] 3.5 Implement full-copy fallback materialization path for non-COW services
- [ ] 3.6 Implement TTL-based snapshot garbage collection
- [ ] 3.7 Implement snapshot rebuild-from-history path (for GC'd snapshots)
- [ ] 3.8 Implement (base, operation) -> output memoization cache and lookup path
- [ ] 3.9 Wire change-detection signal from service responses into chain-advancement logic
- [x] 3.10 Implement session rewind (pointer movement) with truncation-on-new-mutation (design.md D3a). Done: see `docs/impl-plans/0003-session-log.md` - `session.rewindSession` moves `session_pointer.current_sequence` backward only (no deletion at rewind time, taken literally from D3a's own wording); `session.appendEntry` deletes the abandoned forward tail as the first step of its NEXT call, then inserts reusing the freed sequence number. Tests: `test/session/session-log.test.ts` (rewind-without-deletion, truncate-then-reuse-sequence-on-next-append, out-of-range rejection, no-op-at-current-sequence, and a mid-transaction-crash test proving the truncate+insert+advance sequence rolls back as one unit).
- [ ] 3.11 Implement configurable checkpoint interval for intermediate snapshot retention, defaulting to full-chain-for-session-lifetime (design.md D3a)

## 4. Scheduler / placement layer

- [x] 4.1a **NEW (split from 1.10/4.1-4.7):** Promote `archive/placement-resolver/`'s schema (`placement`, `placement_config`, `placement_access`) into `core/`'s consolidated schema (ADR-0002), and its decision/action logic (`resolvePlacement`, `recordAccess`, `evaluatePromotion`, `evaluateDemotion`, `promote`, `demote`, `evictLRUIfOverCapacity`, `effectiveRehydrationCostMs`, `isTrustEligibleForOptimization`) into a new top-level `scheduler/` module (ADR-0007), operating over `core/`'s placement repositories rather than owning any table itself - the one non-trivial promotion ADR-0007 names explicitly, and the fourth and last piece of design.md D6's four-way consolidation. Done: see `docs/impl-plans/0005-placement.md` - `src/core/database/schema.sql` (`placement`/`placement_config`/`placement_access` tables, seeded `'default'` config profile), `src/core/domain/{placement,placement-config}.ts`, `src/core/repositories/{placement,placement-config,placement-access}.repository.ts` + `repositories/queries/*.queries.ts`, `CoreRepos.{placement,placementConfig,placementAccess}`, and the new `src/scheduler/` module (`constants.ts`, `trust.ts`, `placement.ts`). Tests: `test/core/database/schema.test.ts` (extended), `test/core/repositories/{placement,placement-config,placement-access}.repository.test.ts`, `test/scheduler/{placement,trust}.test.ts` (133/133 tests passing repo-wide, real testcontainers-managed Postgres) - covering resolver-miss-is-never-an-error, COALESCE-based partial-update upsert, the declared-prior-until-enough-observed-samples cost authority rule, promotion/demotion hysteresis, batch-never-promotes, pinned-only LRU eviction (unpins, never deletes), the trust-tier gate, and `recordAccess`'s same-transaction atomicity alongside an ordinary `executions` write.
- [ ] 4.1 Implement placement decision service that fuses capability metadata, DSL intent, and runtime observations. **4.1a's `resolvePlacement`/`recordAccess` now provide the runtime-observation leg, and `docs/impl-plans/0007-registry.md` now provides a real `registry/` client (`getPlacementFacts`) for the capability-metadata leg** - this task remains open pending an execution-plan-derived workflow-writer-intent source and a real caller (e.g. `apps/worker`) to fuse all three. **Confirmed still blocked (2026-07, `/impl-package` planning pass for a candidate "placement wiring" package):** `src/workflow-spec/domain/node.ts`'s `Step` carries no `scope`/`mutable`/`interactivity` fields at all - the DSL-intent leg (5.4/5.5) genuinely does not exist yet, not just an unwired caller.
- [ ] 4.2 Implement shared read-only placement path for static/immutable bindings
- [ ] 4.3 Implement affinity hinting (prefer a warm replica) with fallback-to-any-replica rehydration, using the routing mechanism from 1.10. **4.1a's `resolvePlacement` already implements the miss-is-not-an-error/rehydrate-anywhere contract** - this task remains open pending a real caller (`apps/worker`) to wire it into. **Confirmed still blocked (2026-07):** "a real caller" is necessary but not sufficient - `apps/worker` also needs more than one real, addressable replica to prefer/fall back between, which needs a real pool (7.1/7.2), not built. A single-endpoint caller has no affinity decision to make.
- [ ] 4.4 Implement adaptive residency promotion (unpinned -> pinned) based on observed size/frequency. **4.1a's `evaluatePromotion`/`promote` are implemented and tested** - this task remains open pending a scheduling cadence (something calling them periodically) to wire them into. **Confirmed still blocked (2026-07):** a scheduling cadence needs a `contentHash` to evaluate, and per D2/D3 that hash is over *materialized session state* - session materialization (3.2/3.3, the snapshot store) doesn't exist yet, so there is no real content-addressed value in `apps/worker`'s current (light-bindings-only, 6.2a) dispatch path to key a placement row on.
- [ ] 4.5 Implement promotion/demotion thresholds per design.md D4a's cache-admission model, exposed as tunable scheduler parameters (starting defaults, not fixed constants). **4.1a implements this as data (`placement_config`) plus every decision function's `config` parameter** - this task's remaining scope is picking a source-of-truth/reload strategy beyond the single `'default'` profile.
- [ ] 4.6 Implement capacity-aware LRU eviction among pinned entries when the pinned-residency budget is exceeded (design.md D4a). **4.1a's `evictLRUIfOverCapacity` is implemented and tested** - this task remains open pending a scheduling cadence to run it on and a real capacity budget sourced from autoscaling. **Confirmed still blocked (2026-07):** same `contentHash`-needs-session-materialization gap as 4.4, plus 7.x's real capacity-budget signal.
- [ ] 4.7 Wire trust-tier gating (2.5) into placement decisions - never share/pool/COW-reuse below production-proven (design.md D5a). **4.1a's `isTrustEligibleForOptimization` gate is promoted and tested, and `docs/impl-plans/0007-registry.md` now provides `getPlacementFacts` (2.8) as a real module to source the trust tier from** - this task remains open pending the actual gate placement at share/pool/reuse call sites (needs a real caller, e.g. `apps/worker`). **Confirmed still blocked (2026-07):** `apps/worker` (0011) has no share/pool/COW-reuse call site to gate at all - every dispatch is spawn-per-call against one statically configured endpoint (0011's "Addressing gap"); this task needs 7.x's pooling to exist before there is anything for the gate to guard.
- [ ] 4.8 **CORRECTED SCOPE (design.md D17a, `docs/adr/0008`):** implement CLI heavy-data transport volume/local-state attach-on-promote, detach-on-demote for **every warm function** (all step dispatch is CLI now, not a "CLI-invoked" subset), reusing 4.4-4.6's promotion/demotion hooks rather than a new lifecycle concept (design.md D17/D17a). For local state living on a container's own writable layer (D17's scratch-file fallback, as opposed to a per-hash CSI volume), detach-on-demote is realized via the exec-agent's `Evict(stateId)` verb (`docs/adr/0008`) - a per-hash CSI volume needs no such call, since Kubernetes handles attach/detach independently of the agent

## 5. Workflow DSL

- [x] 5.1 Define the workflow-spec/execution-plan schema (steps, bindings, write targets, secret refs, outputs) per design.md D8 - engine-agnostic. Done: see `docs/impl-plans/0004-workflow-spec-schema.md` - new top-level `workflow-spec/` module (ADR-0007/0012, renamed from `ir/`; a separate `execution-plan/` module splits out once `dsl-compiler/` exists - see `docs/glossary.md` and ADR-0003's naming note): `src/workflow-spec/domain/{workflow-spec,node,binding,session-state,write-target,secret-ref,placeholder-types}.ts` (the `Binding` discriminated union, all 8 kinds incl. `itemResource` per D16, promoted from ADR-0003's contract; `workflow-spec.ts` also exports a temporary `ExecutionPlan = WorkflowSpec` alias, flagged for splitting once a real compiler exists), `src/workflow-spec/schema/workflow-spec.schema.json` (promoted from `archive/dsl/schema/`, task 1.7, extended with `itemResource`; version field renamed `workflowSpecVersion`, was `irVersion`), `src/workflow-spec/validate.ts` (ajv-backed, never throws - returns `{valid, errors}`), `src/workflow-spec/constants.ts`, `src/workflow-spec/index.ts` barrel. `migrate()`/`deriveSignature()` (ADR-0003's other named contract pieces) and 5.2/5.3/5.9-5.13b are explicitly deferred to future packages - see the plan doc's Scope section. Tests: `test/workflow-spec/validate.test.ts` (46 tests: whole-document fixtures ported/extended from `archive/dsl/schema/examples{,-invalid}/` plus 14 targeted test cases TC-1 through TC-14), `test/workflow-spec/domain.test.ts` (1 compile-time exercise) - 107/107 tests passing repo-wide (plain Vitest, no testcontainers needed - the first genuinely I/O-free package in this repo).
- [x] 5.2 Implement an authoring-surface-to-execution-plan compiler for the syntax chosen in 1.7. Done: see `docs/impl-plans/0009-dsl-compiler-plain-steps.md` - new top-level `dsl-compiler/` module (ADR-0007), `src/dsl-compiler/compile.ts`'s `compile(input, { registryPool })`: restricted-YAML parse (5.6a) -> `workflow-spec/`'s existing `validate()` -> document-wide semantic checks (`src/dsl-compiler/semantic-validation.ts` - step-id uniqueness, `dependsOn`/`{from:"step"}` reference resolution, and D8c's branch-case/map-body internal-id-visibility rule, all applied to the full grammar including nested `branch`/`map`) -> registry checks (5.3). Returns `{ ok: true, executionPlan }` (still byte-identical to the `WorkflowSpec`, per 0004's alias) or `{ ok: false, errors }` - never throws for an invalid document. `branch`/`map` themselves remain unexecuted (6.2b) - this package compiles/validates the full grammar but does not run it. Tests: `test/dsl-compiler/{restricted-yaml,semantic-validation,compile}.test.ts`.
- [x] 5.3 Implement validation against the registry (reject unknown service/function references) at execution-plan-compile time. Done: see `docs/impl-plans/0009-dsl-compiler-plain-steps.md` - `src/dsl-compiler/registry-validation.ts`'s `validateServiceReferences(spec, registryPool)`, called by `compile()` (5.2) for every `Step` anywhere in the document (including inside `branch`/`map`), via `registry/`'s existing `getEntry` client (never its schema, per ADR-0006/0007). Tests: `test/dsl-compiler/compile.integration.test.ts` (real testcontainers-managed registry + core Postgres, including a full YAML-to-`engine/submitRun`-to-completion run proving the compiled `executionPlan` is exactly what `engine/` already knows how to execute).
- [ ] 5.4 Implement data-binding syntax for user/static/session source references
- [ ] 5.4a Implement the `sessionState` declaration block (interactivity + fallback, declared once per key, resolved by every binding referencing that key) per design.md D8a
- [ ] 5.5 Implement scope/mutable declaration syntax per binding, and `writes` declarations gated by change-detection
- [ ] 5.6 Implement dependency-graph inference from step-output bindings, plus an explicit `dependsOn` ordering escape hatch
- [x] 5.6a Implement the restricted-YAML/JSON parser + JSON Schema validation (no anchors/aliases/merge keys/custom tags; camelCase fields) per design.md D8a. Done: see `docs/impl-plans/0009-dsl-compiler-plain-steps.md` - `src/dsl-compiler/restricted-yaml.ts`'s `parseRestrictedYaml(source)`: parses with the `yaml` package, then walks the AST rejecting any node with an anchor, any alias, any `<<` merge key, and any custom tag (D8a) - string input only; non-string (already-parsed) input skips this stage, per the plan doc's own decision. JSON Schema validation itself was already 0004's `validate()`; this task's own JSON-Schema-validation half is exercised by `compile()` calling it, not re-implemented here. Tests: `test/dsl-compiler/restricted-yaml.test.ts`.
- [ ] 5.6b Implement the `{from: item}` binding source for map/forEach bodies
- [ ] 5.6c Implement flat request-parameter binding validation (reject any dotted/nested path at the binding-source level)
- [ ] 5.6d Implement the dataset URN parser/resolver (namespace/name, tag-or-digest) and stand up the dataset resource catalog: a thin tag→digest→object-key index backed by dedicated object storage (design.md D8b) - distinct from the container/OCI registry, and not itself an artifact-registry client
- [ ] 5.6d-i Select the specific object storage product (S3/GCS/MinIO-compatible) for dataset bytes (open question, design.md D8b)
- [ ] 5.6e Implement OCI reference validation for pinned service-version calls, rejecting dataset URNs used in that position and vice versa
- [ ] 5.7 Implement `branch` construct (statically enumerated cases, dynamically selected), including per-case `yields` resolution and rejection of direct references to a case's internal step ids (design.md D8c)
- [ ] 5.8 Implement `map`/`forEach` construct (statically shaped body, dynamically sized cardinality), including `yields` collection into parallel arrays and rejection of direct references to body-internal step ids (design.md D8c)
- [ ] 5.9 Implement derived workflow-signature generation and publish it through the workflow-spec store's discovery mechanism (see 11.2) - not the service registry, which indexes service images only
- [ ] 5.10 Implement execution-plan-to-execution-engine compilation targeting the generic interpreter from 1.5, once the engine is selected (1.4)
- [ ] 5.11 Choose JSON-Logic vs. CEL against real branch/map cases (open question, design.md D10)
- [ ] 5.12 Implement `compute` binding evaluation (in-interpreter, no step-execution scheduling, no secret inputs permitted)
- [ ] 5.13 Implement the `workflowSpecVersion` tag and lazy forward-only migration chain per design.md D11
- [ ] 5.13a Implement fail-closed handling for documents newer than the reader understands (design.md D11)
- [ ] 5.13b Define the minimum supported version window and the migration-sweep process required before retiring an old migrator (design.md D11)
- [ ] 5.14 Implement the generic "step binding satisfies every required parameter" validation rule (design.md D9c relies on this instead of an agent-specific construct)
- [ ] 5.15 Implement the `itemResource` binding kind - `{ from: itemResource, itemId: <binding>, path: <locator> }` - resolving at run time against the per-instance flattened cache from 12.4 into either a `static`-equivalent dataset reference or a plain passed-through value (design.md D16)
- [ ] 5.16 Implement JSON-Pointer-style (RFC 6901, deeply nested) path addressing for `itemResource` bindings, deliberately distinct from 5.6c's flat-only `request`-parameter rule (design.md D16)

## 6. Execution engine

- [x] 6.1 **SPLIT into 6.1a/6.1b** (see `docs/impl-plans/0001-durable-core.md`) - was marked done, then re-opened (nothing described below was ever actually committed to this repo), now split into two genuinely separable deliverables so each has its own buildable, testable scope rather than one task bundling both. Per 1.4a/design.md D6a, both start from spike 1.2's schema.sql/worker.js (now `archive/spikes/1.2-resonate-pg-durable-exec/`) as the actual base, not a from-scratch fork of resonate-pg or hatchet-dev's tutorial. **Target architecture is the single-package structure decided in `docs/adr/0001`/`0007`/`0009` - NOT the npm-workspaces `packages/` layout this task originally described.** Tooling for both is `docs/adr/0009`/`0010`'s single-package TypeScript setup (Biome, Vitest + testcontainers-node, raw `pg`, esbuild for app bundling) - explicitly NOT npm workspaces, `tsc -b` project references, or a `packages/` directory, superseding this task's original (also never-committed) description.
- [x] 6.1a Promote spike 1.2's `executions`/`checkpoints` tables and its no-separate-sweeper `claim_execution()` dispatcher (`SELECT ... FOR UPDATE SKIP LOCKED`) into `core/`'s schema (ADR-0002), with `engine/` exposing `claimExecution`/`completeExecution` as two composable primitives (not a single hardcoded `processOneExecution`) so `session/`, `scheduler/`, and `dataset-catalog/` can interleave their own writes on the same transaction, per ADR-0002's consolidation model. Deliberately NOT promoted into `engine/` itself: the spike's `session_log`/`session_pointer` (D3, task 3.1, belongs in `core/`+`session/`) and `placement` (D4, belongs in `core/`+`scheduler/` per the `archive/placement-resolver/` split, `docs/adr/0007`) - those tables existed in the spike only to prove the DEEP-consolidation claim, a proof already recorded in `archive/spikes/1.2-resonate-pg-durable-exec/FINDINGS.md`. Done: see `docs/impl-plans/0001-durable-core.md` - `src/core/database/{schema.sql,connection-pool,transactions}.ts`, `src/core/domain/{execution,checkpoint,rows,mappers}.ts`, `src/core/repositories/{executions,checkpoints}.repository.ts` + `repositories/queries/*.queries.ts`, `src/engine/{claim-complete,index}.ts` (module-internal structure per `docs/adr/0012`, refactored from this task's original flatter layout); base repo scaffolding (`package.json`/`tsconfig.json`/`biome.json`/`.nvmrc`/`vitest.config.ts`/`docker-compose.dev.yml`), the shared `src/shared/{config,errors}.ts` and `src/shared/observability/logger.ts` (ADR-0009, paths per ADR-0012) stood up alongside it since none had a prior home. Tests: `test/core/database/{schema,transactions}.test.ts`, `test/core/repositories/{executions,checkpoints}.repository.test.ts`, `test/engine/claim-complete.test.ts`, `test/shared/{config,errors}.test.ts`, `test/shared/observability/logger.test.ts` (21/21 passing against real, ephemeral testcontainers-managed Postgres, plus plain unit tests for config/errors) - covering schema structure, claim contention (`SKIP LOCKED`), lease-expiry reclaim, mid-transaction-crash rollback-to-`queued` (`pg_terminate_backend`, mirroring spike 1.2's own crash test), checkpoint exactly-once idempotency, `withTransaction` atomicity, the claim/complete composability shape (an interleaved third write on the same transaction), fail-closed config parsing, and structured-error `errorId`/context propagation.
- [x] 6.1b **NEW (split from 6.1)** Still to build on top of 6.1a: durable sleep (a `waits` table; `claim_execution()` claiming a due timer wait via an `EXISTS` join, no separate sweeper; a `signal_wait()` that durably broadcasts to every execution waiting on a key) and LISTEN/NOTIFY (a `WakeListener` modeled explicitly as a latency optimization that is never a correctness dependency, per the same posture spike 1.2 already established for affinity/D4), informed by, not copied from, resonate-pg and hatchet-dev's designs. Done: see `docs/impl-plans/0002-durable-sleep.md` - `src/core/database/schema.sql` (`waits` table, `executions.status` gains `'waiting'`, `claim_execution()`'s due-timer-wait `EXISTS` branch, new `signal_wait()` function), `src/core/domain/wait.ts`, `src/core/repositories/waits.repository.ts` + `repositories/queries/waits.queries.ts`, `src/core/repositories/executions.repository.ts`'s `markWaiting`, `src/core/database/wake-listener.ts` (`createWakeListener`, standalone, not yet wired to a poll loop - none exists yet), `src/engine/wait.ts` (`waitFor`/`signalWait`). Tests: `test/core/database/schema.test.ts` (extended), `test/engine/wait.test.ts`, `test/core/repositories/waits.repository.test.ts`, `test/core/database/wake-listener.test.ts` (34/34 tests passing across 11 files, real testcontainers-managed Postgres) - covering timer-wait/signal-wait/hybrid-wait claim semantics, broadcast-to-every-matching-wait, mid-transaction-crash rollback, and LISTEN/NOTIFY delivery/unsubscribe.
- [x] 6.2 **SPLIT into 6.2a/6.2b** (see `docs/impl-plans/0006-interpreter-plain-steps.md`) - implicitly bundled plain steps, `branch`, `map`, and every binding kind (including ones whose backing infrastructure doesn't exist yet) into one task; split so each half has its own buildable, testable scope, mirroring how 6.1 was split into 6.1a/6.1b.
- [x] 6.2a **NEW (split from 6.2):** a generic dependency-graph interpreter for a `WorkflowSpec` whose `steps` are all plain `Step` nodes (no `branch`/`map`) - dependency inference from `{from:"step"}` bindings plus the `dependsOn` escape hatch (5.6, for the plain-step case), binding resolution for `request`/`step`/`literal` kinds only (5.4, partial), and run-to-completion with resolved top-level `outputs`, dispatching steps via an injected `StepDispatcher` (real dispatch is 6.3/6.4/6.12-6.15, not this task). Done: see `docs/impl-plans/0006-interpreter-plain-steps.md` - `src/core/database/schema.sql` (`workflow_runs`/`run_node_outputs` tables, `executions` gains `run_id` + a `'blocked'` status), `src/core/domain/{workflow-run,run-node-output}.ts`, `src/core/repositories/{workflow-runs,run-node-outputs}.repository.ts` + `repositories/queries/*.queries.ts`, `src/core/repositories/executions.repository.ts`'s `enqueueForRun`/`promoteBlockedToQueued`, `CoreRepos.{workflowRuns,runNodeOutputs}`, and the new `src/engine/{dependency-graph,bindings,interpreter}.ts` (`submitRun`/`promoteReadyNodes`/`completeStep`/`resolveStepReads`/`findRunStepNode`/`getRunResult`), composing 0001's `claimExecution`/`completeExecution` unmodified. Tests: `test/core/database/schema.test.ts` (extended), `test/core/repositories/{workflow-runs,run-node-outputs}.repository.test.ts`, `test/engine/{dependency-graph,bindings,interpreter}.test.ts` (164/164 tests passing across 26 files, real testcontainers-managed Postgres) - covering dependency ordering enforced from DB state alone before any worker runs, same-transaction promotion, a full multi-worker run to completion with resolved `outputs`, explicit fail-closed errors for unsupported node kinds and binding kinds, and a mid-transaction-crash test proving checkpoint+run_node_outputs+promotion roll back together.
- [ ] 6.2b **NEW (split from 6.2, deferred):** `branch` (5.7)/`map`/`forEach` (5.8, 6.9) nodes, multi-step nesting inside case/map bodies, and `session`/`static`/`item`/`compute`/`itemResource` binding resolution (the remainder of 5.4, plus 5.12/5.15/5.16) - each needs infrastructure that doesn't exist yet (session materialization 3.2-3.9, the dataset catalog 5.6d, item-pool 12.x, or 5.11's still-open JSON-Logic-vs-CEL decision). **6.2a's `run_node_outputs` table and dependency-promotion mechanism (`docs/impl-plans/0006-interpreter-plain-steps.md`) are the now-ready base this task builds on**, deliberately scoped from the start to top-level node ids only per D8c.
- [x] 6.3 Implement step-execution handling for spawn-per-call (World 1) execution. Per `docs/adr/0005`/`0008`: realized via the in-pod exec-agent's `Invoke` RPC against a freshly-started Pod, not a REST call - the same mechanism as 6.4, differing only in whether the target Pod was already warm. Done: see `docs/impl-plans/0011-worker-cli-dispatch.md` - `apps/worker` (6.3/6.4/6.15 built as ONE dispatch code path, per ADR-0005/ADR-0008: World 1 vs. World 2 is a placement/pooling distinction this package deliberately does not make yet - see its Scope). `src/apps/worker/dispatch.ts`'s `dispatchStep`/`buildInvokeRequest` calls the exec-agent's real `Invoke` RPC for every step (light bindings only). Tests: `test/apps/worker/worker-loop.integration.test.ts` (T7-T11, against a REAL spawned agent binary + `agent/testdata/fake-cli.sh`, no mock).
- [x] 6.4 Implement step-execution handling for warm-pooled-service (World 2) invocation. Per `docs/adr/0005`/`0008`: realized via the in-pod exec-agent's `Invoke` RPC against an already-warm, pinned Pod (addressed via the placement table's replica mapping, per D4), omitting `--data-file` when the target already holds the needed `--state-id` locally (design.md D17a). Done: see `docs/impl-plans/0011-worker-cli-dispatch.md` - same dispatch code path as 6.3 (see that entry). Placement-aware addressing to a specific warm replica (4.1/4.3/4.4-4.7) is explicitly NOT built here - this package uses a single static `AGENT_INVOKE_BASE_URL` (0011's "Addressing gap") pending a real multi-replica pool (7.x).
- [ ] 6.5 Implement session-as-long-running-execution pattern with event/signal-driven user actions
- [ ] 6.6 Configure native retry/backoff/timeout policies per step
- [ ] 6.7 Implement step-level memoization skip using the cache from 3.8
- [ ] 6.8 Implement a narrow "run as a spawned job" step type for delegating select World-1 steps to a secondary backend (e.g. Kubernetes Jobs), if the selected engine benefits from this delegation
- [ ] 6.9 Implement dynamic child/step execution for `map` constructs (parent continues without terminating; independent per-child retry)
- [ ] 6.10 Implement scheduler pre-analysis of all statically declared branch cases and map-iteration shapes ahead of execution
- [ ] 6.11 Implement bounded agent-directed step execution: allowlist enforcement, governor enforcement, durable/resumable multi-round tool-calling loop
- [x] 6.12 **NEW (`docs/adr/0008`):** build the in-pod exec-agent (separate Go module per `docs/adr/0001`/`0009`): the `Invoke`/`Evict` RPC server, running as the container's entrypoint (PID 1, not a sidecar), fork/exec'ing the service author's original CLI entrypoint on request. Done: see `docs/impl-plans/0010-exec-agent.md` - `agent/` (separate Go module, own `go.mod`), `agent/main.go` (PID-1 entrypoint with SIGTERM graceful shutdown), `agent/internal/server/{server,invoke,evict}.go` (HTTP/JSON `Invoke`/`Evict`, per this package's wire-format call), `agent/internal/execrunner/execrunner.go` (fork/exec translating `Args`/`DataFiles`/`Secrets`/`Stdin`/`TimeoutMs` into the real subprocess call). Tests: `agent/internal/execrunner/execrunner_test.go`, `agent/internal/server/server_test.go` (`go test ./... -race`, all passing).
- [x] 6.13 **NEW (`docs/adr/0008`):** build the platform-controlled injection mechanism (an init-container copying the static agent binary onto a shared `emptyDir`, plus the Pod-template command override) so onboarding a service requires no author-side Dockerfile changes. Done: see `docs/impl-plans/0010-exec-agent.md` - `agent/build/Dockerfile` (static-binary init-container image, verified via `docker build`), `agent/deploy/pod-injection-example.yaml` (documented generic shape) and `agent/deploy/kind/` (a real, ephemeral `kind` cluster end-to-end test - `run-e2e.sh`, `test-pod.yaml`, `fake-service.Dockerfile`) proving the actual claim against a real kubelet/API-server: the init-container copies the binary onto a shared `emptyDir`, the target Pod's command override starts the agent as PID 1 against a target image (`fake-service:test`) containing zero agent/platform code, and the agent serves real `/invoke`/`/evict` calls from inside that Pod. Test: `agent/deploy/kind/run-e2e.sh` (run and passing - "T14 PASSED").
- [x] 6.14 **NEW (`docs/adr/0008`):** implement the exec-agent's local idempotency/dedup, keyed on `(executionId, stepId)` - the same tuple as the `checkpoints` table's uniqueness constraint - covering the in-pod race between "still running here" and a second caller retrying the same invocation after a dropped connection or a crashed worker. Done: see `docs/impl-plans/0010-exec-agent.md` - `agent/internal/dedup/dedup.go` (`Store.Attach`/`Resolve`, leader/follower semantics, TTL-bounded retention, plus a `maxLease`-bounded sweep force-resolving a dead leader's entry so followers never wedge forever). Tests: `agent/internal/dedup/dedup_test.go` (concurrent-same-key leader-uniqueness test, dead-leader force-expiry test) and `agent/internal/server/server_test.go`'s `TestInvoke_concurrentSameKeyDedup`/`TestInvoke_newInvocationAfterDedupWindow` (real HTTP-level dedup, asserting exactly one subprocess spawn under concurrency, `-race` clean).

  **Post-review hardening (local code review pass, `docs/impl-plans/0010-exec-agent.md`'s Implementation notes):** path-traversal rejection and real-failure reporting in `Evict`; argument-injection validation on translated CLI flags; an optional shared-secret bearer-token auth middleware; leader-exec detached from the inbound HTTP request's context; `timeoutMs` validation (positive, capped); bounded request-body/output-buffer sizes; HTTP server read/write/idle timeouts; hardened (non-root, resourced, probed) Pod manifests with an automated flag-shape drift check between the tested and documented manifests. Re-verified: `go test ./... -race` (29 cases) and a fresh `agent/deploy/kind/run-e2e.sh` run, both green.
- [x] 6.15 **NEW (`docs/adr/0008`):** wire `apps/worker`'s outer dispatch (design.md D17a) to call the exec-agent's `Invoke` RPC for every step, addressing a pinned replica directly (via the placement table, D4) with any-replica fallback on a miss. Done: see `docs/impl-plans/0011-worker-cli-dispatch.md` - the first implementation of `apps/worker` (`src/apps/worker/{main,config,constants,agent-client,dispatch,worker-loop}.ts`), generalizing `test/dsl-compiler/compile.integration.test.ts`'s proven manual claim/dispatch/complete loop into a real, run-id-agnostic worker loop (`runOnce`/`runWorkerLoop`) that dispatches via the exec-agent's real `Invoke` RPC instead of an in-memory stub. **Partial**: "addressing a pinned replica directly... with any-replica fallback" is NOT implemented - there is no real multi-replica pool yet (`Placement.replicaId` is a bare opaque string with no network address anywhere in `core/`/`scheduler/`/`registry/`); every dispatch uses one statically configured `AGENT_INVOKE_BASE_URL` instead, an explicit, documented placeholder (see 0011's "Addressing gap") pending 7.x's pool work. Also added: `core/`'s `ExecutionsRepo.markFailed` (a small, justified addition - 'failed' was already a valid `executions.status` value with no writer). Tests: `test/apps/worker/{dispatch,agent-client,config}.test.ts` (unit), `test/apps/worker/worker-loop.integration.test.ts` (T7-T11, real testcontainers Postgres + a REAL spawned exec-agent binary, no mock), `test/core/repositories/executions.repository.test.ts` (extended, `markFailed`).

## 7. Autoscaling & pooling

- [ ] 7.1 Define KEDA scaling rules per service. Per `docs/adr/0008`'s finding: use KEDA's native PostgreSQL scaler reading `core/`'s `executions` table directly (queued/blocked-execution count per service digest) rather than an in-pod/HTTP metrics endpoint - a direct consequence of the D6 consolidation decision, not a separate metrics mechanism to build
- [ ] 7.2 Implement pre-warmed pool management per service. Per `docs/adr/0008`: a pre-warmed instance is a Pod running the in-pod exec-agent as PID 1, kept ready to receive `Invoke` calls - not a Pod behind a Service answering REST requests (superseded framing)
- [ ] 7.3 Implement pooled-instance reuse gating (provably stateless OR scoped to a content hash before reuse). Ties into the exec-agent's own local idempotency/dedup keyed on `(executionId, stepId)` (`docs/adr/0008`) as a purely in-pod safety net, distinct from and complementary to this gating's correctness guarantee
- [ ] 7.4 Implement scale-in protection for instances pinned to actively-used session state

## 8. Integration & validation

- [ ] 8.1 End-to-end test: static, immutable dataset shared and pooled across concurrent workflow instances
- [ ] 8.2 End-to-end test: session mutates data, chain advances, isolation from other sessions verified
- [ ] 8.3 End-to-end test: worker failure mid-workflow, resume without duplicate side effects
- [ ] 8.4 End-to-end test: warm replica unavailable, rehydration path serves the request correctly
- [ ] 8.5 End-to-end test: snapshot GC'd then rebuilt from session history on demand
- [ ] 8.6 Load test: demand spike triggers autoscaling without violating isolation guarantees
- [ ] 8.7 End-to-end test: map construct over a runtime-sized collection, with one iteration failing and retrying independently
- [ ] 8.8 End-to-end test: branch construct selects correct case at runtime; scheduler pre-analysis covers all declared cases
- [ ] 8.9 End-to-end test: agent-directed step refuses a call outside its allowlist and stops at its governor limit
- [ ] 8.10 End-to-end test: session rewind followed by a new mutation discards the abandoned forward tail (design.md D3a)
- [ ] 8.11 End-to-end test: an unverified service build is never shared/pooled even when it declares COW support; promotion after conformance passes (design.md D5a)
- [ ] 8.12 End-to-end test: an older workflow-spec document migrates on open; a too-new document fails closed (design.md D11)

## 9. Secrets management

- [ ] 9.1 Define secret scope model (writer / user) and reference syntax in the DSL
- [ ] 9.2 Integrate a broker-agnostic secrets store interface (concrete product from 1.6)
- [ ] 9.3 Implement worker-side, in-step-execution secret resolution (references only in durable history). Per `docs/adr/0008`: resolution happens in the worker, immediately before calling the exec-agent's `Invoke` RPC - only the reference is ever recorded in durable history, the resolved value is never persisted anywhere outside that one RPC call
- [ ] 9.4 **CORRECTED MECHANISM (`docs/adr/0008`):** implement per-request injection into service invocations (no env-var binding) via the exec-agent's `Invoke` RPC payload - this RPC *is* "the request" D7 rule 3 describes, now that step dispatch is CLI-only and there is no HTTP body to push a secret into; the agent decides the OS-level delivery (tmpfs file, stdin, or a subprocess-scoped env var, never the container's whole lifetime) for the lifetime of one `exec()` call
- [ ] 9.5 Bind user-secret lifetime to session TTL with reference re-resolution on replay
- [ ] 9.6 Add payload-at-rest encryption (if the selected engine offers a codec hook) and log redaction
- [ ] 9.7 Exclude secret-consuming external calls (including LLM/agent API calls) from the memoization cache (wire into 3.8)
- [ ] 9.8 End-to-end test: pooled container serving different workflows never observes a foreign secret

## 10. Service nesting

- [ ] 10.2 Wire the registry's `nesting_declaration: { via: sdk|http|cli|mcp, targets: [...] | open }` field (2.1c) into the nesting-enforcement layer (design.md D9b)
- [ ] 10.3 Implement the mandatory-by-default orchestrator-aware nesting path for services that declare `nesting_declaration`
- [ ] 10.4 Implement the declared-exception path for services that bypass the orchestrator-aware path, wired into trust-tier review (2.5-2.7, design.md D5a/D9b)
- [ ] 10.5 Implement dispatch-time allowlist enforcement and secret withholding for open-target nesting services (design.md D9c)
- [ ] 10.6 Implement the durable governor counter (count/cost/timeout), checked before each dispatch, surviving crash-and-resume (design.md D9c)
- [ ] 10.7 Exempt pure `compute`-backed tools from allowlist review (design.md D9c)
- [ ] 10.8 Implement the MCP gateway: translate an invocation's allowlisted OpenAPI operations into scoped MCP tool definitions, routing calls through 10.5/10.6 (design.md D9c)
- [ ] 10.10 End-to-end test: an agent-runner service (`nesting_declaration: {via: mcp, targets: open}`) refuses an out-of-allowlist call and halts at its governor limit, surviving a mid-loop crash without resetting the counter
- [ ] 10.11 End-to-end test: an undeclared direct-transport bypass is detected and treated as a capability-declaration violation (design.md D5a)

## 11. Workflow-spec store

- [ ] 11.1 Implement the workflow-spec store: URN identity (`urn:workflow-platform:workflow:ns/name[:tag|@digest]`) + immutable-version keying, storing a workflow-spec's execution plan + authoring doc (design.md D13)
- [ ] 11.2 Implement derived-signature discovery/query surface for published workflow-specs (design.md D13; consumed by 5.9)
- [ ] 11.3 Implement the fork operation: copy a source workflow-spec's shape/steps into a new, self-contained workflow-spec under the forking writer's namespace (design.md D9a/D13)
- [ ] 11.4 Implement the immutable, transitive fork-lineage pin (`forkedFrom: urn:...@version`), inspectable through chained forks, never a run-time resolution dependency (design.md D13)
- [ ] 11.5 Implement the hard platform invariant: writer-scoped secret references inherited from a source workflow-spec do not resolve under the forking writer's identity; treat as an unsatisfied binding until re-bound (design.md D7/D13)
- [ ] 11.6 Confirm static-dataset references carry over unchanged on fork by default, with no platform-level namespace/visibility gating (design.md D13)
- [ ] 11.7 Confirm dynamic `map`/`forEach` fan-out continues to use tracked child executions, unrelated to and unaffected by forking (design.md D8/D9a/D13)
- [ ] 11.8 Document fork-lineage-cycle handling as a known, deferred, non-execution-affecting limitation (design.md D13)
- [ ] 11.9 Define how the (external) authoring tool surfaces a workflow-spec-version mismatch between a source workflow-spec and a forking writer's own work, rather than the platform silently migrating or rejecting (design.md D11/D13)
- [ ] 11.10 Document that visibility/tenancy/publish-authority are explicitly delegated to the external authoring tool and are not enforced by the platform (design.md D13)
- [ ] 11.11 End-to-end test: a fork is self-contained and executes correctly with no run-time dependency on its source workflow-spec, even after the source publishes a new version
- [ ] 11.12 End-to-end test: a fork containing an unresolved inherited writer-scoped secret reference is treated as an unsatisfied binding; re-binding it to the forking writer's own secret makes the fork valid
- [ ] 11.13 End-to-end test: fork lineage remains inspectable and transitive across a chain of forks

## 12. Item Pool integration

- [ ] 12.1 Select/confirm the concrete Item Pool integration protocol and product (open question, design.md D15 Open Questions)
- [ ] 12.2 Implement per-item-instance resolution: on first reference anywhere on the platform, fetch the full manifest for `itemInstanceId` from the Item Pool (design.md D15)
- [ ] 12.3 Implement the eager whole-manifest flattening walk: classify each leaf as dataset-shaped (mirror bytes into the existing dataset resource catalog via 5.6d/D8b, minting a URN) or plain-value (pass through inline), trusting the Item Pool's own self-description with no tiered trust model (design.md D16)
- [ ] 12.4 Implement the memoized `itemInstanceId -> { path -> resolved dataset-reference | value }` cache, rebuildable by re-querying the Item Pool, TTL/GC-safe under the same posture as 3.6/3.7 (design.md D15)
- [ ] 12.5 Feed dataset-shaped leaves from 12.3 into the existing static-dataset materialization/placement path (3.4/3.5/4.1-4.7) completely unchanged - no parallel materialization mechanism
- [ ] 12.6 End-to-end test: two learners resolving the same item instance share the mirrored dataset (single fetch/mirror, cache hit on the second reference); two learners on different instances of the same item type remain isolated
- [ ] 12.7 End-to-end test: an item instance's resolved resource map stays stable for the life of one learner attempt/session, per design.md D15's confirmed instance-stability requirement
- [ ] 12.8 End-to-end test: a transient Item Pool outage is covered by ordinary step retry/backoff (D6/R7), with no bespoke Item-Pool-specific retry logic
- [ ] 12.9 Document that item confidentiality/visibility and item-type/instance shape consistency are delegated entirely to the Item Pool and external authoring tooling - not platform invariants (design.md D16)
